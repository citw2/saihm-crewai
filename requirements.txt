# Core client needs only `mcp` (drives the Node sidecar over MCP stdio).
mcp>=1.0
# CrewAI adapter (version pinned to what this demo was verified against):
crewai>=1.14,<2      # for saihm_memory.SaihmStorageBackend (crewai StorageBackend)
